# Mutation report — `skill/src/classify.ts`

> Generated by [Stryker](https://stryker-mutator.io/) (vitest runner) on 2026-06-29. Stryker mutates the source and re-runs the full 800-test suite per mutant; a *killed* mutant is one a test caught. Config: `stryker.conf.json`. Re-run: `npm run mutation`. This is honest robustness signal, reported as-is — **not** inflated to a target.

## Scope

`skill/src/classify.ts` — the core danger classifier (the heart of the SIGN/HOLD/REJECT decision). This is the highest-value file to mutation-test; the same config can be widened to `verdict.ts`/`decode.ts`/`roles.ts`/`outflow.ts`.

## Result

| Metric | Value |
|---|---|
| Mutants | 823 |
| Killed | 481 |
| Survived | 304 |
| No coverage | 38 |
| Timeout | 0 |
| **Mutation score (total)** | **58.4%** |
| **Mutation score (covered)** | **61.3%** |

## Honest interpretation

**172 of the 342 survivors+no-coverage are `StringLiteral` mutations** — i.e. changing verdict `reason` / `mapsToLoss` *message text*. Tests deliberately do **not** assert exact human-readable wording (that would be brittle), so these are largely *equivalent* mutants, not coverage gaps.

**Excluding string-literal mutants, the behavioral kill rate is 70.2% (400 / 570).**

The remaining **logic survivors are a real improvement target** (not hidden):

| Mutator (survived/no-cov) | Count |
|---|---|
| ConditionalExpression | 92 |
| EqualityOperator | 21 |
| ObjectLiteral | 18 |
| LogicalOperator | 17 |
| BlockStatement | 8 |
| ArithmeticOperator | 5 |
| MethodExpression / OptionalChaining / ArrayDeclaration / BooleanLiteral | 9 |

These are conditionals/branches in `classify.ts` whose negation isn't caught by an assertion — future test work should target them.

## Complement: the fuzz harness

The critical **fail-closed invariant** is proven separately and exhaustively by `skill/test/fuzz-fail-closed.test.ts`: across all 44,288 single-byte mutations of an authority-transfer fixture, **zero** yield SIGN (and disabling the GAP-FC1 fix yields 243 SIGN at the SPL discriminator byte — the property is genuinely falsifiable). Mutation testing measures *general* test sensitivity; the fuzz scan guarantees the *one property that matters most* (a mutated authority byte never becomes SIGN).
